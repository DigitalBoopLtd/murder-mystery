## Murder Mystery Game - Environment configuration
#
# Copy this file to `.env` and fill in the values below.
# At minimum, you must provide a valid `OPENAI_API_KEY`.

# -----------------------------------------------------------------------------
# Core API keys
# -----------------------------------------------------------------------------

# Required: OpenAI API key for LLMs and Whisper transcription.
OPENAI_API_KEY=your_openai_api_key_here

# Optional: ElevenLabs API key for high-quality TTS and suspect voices.
# If not set, the game will still run, but you will only see text responses.
ELEVENLABS_API_KEY=your_elevenlabs_api_key_here

# Optional: HuggingFace token for 90s adventure-game style images
# (portraits, opening scene, and location art).
# If not set, the game will run without generated images.
HF_TOKEN=your_huggingface_token_here

# -----------------------------------------------------------------------------
# Advanced model overrides (optional)
# -----------------------------------------------------------------------------

# Optional: override the model used to resolve suspect-related actions.
# Defaults to `gpt-4o-mini` when unset.
SUSPECT_RESOLVER_MODEL=gpt-4o-mini

# Optional: override the model used to resolve location/search actions.
# Defaults to `gpt-4o-mini` when unset.
LOCATION_RESOLVER_MODEL=gpt-4o-mini


