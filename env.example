# =============================================================================
# Murder Mystery Game - Environment Configuration
# =============================================================================
#
# Copy this file to `.env` and fill in your API keys.
#
# For LOCAL DEVELOPMENT:
#   - Set your keys here and they'll be used automatically
#   - The Settings tab will show "✅ Environment" for each configured key
#
# For DEPLOYED MODE (HuggingFace Spaces, etc.):
#   - Users enter their own keys via the 🔑 Settings tab in the UI
#   - Keys are stored in session memory only (never persisted)
#   - Don't commit real API keys to your repository!
#
# =============================================================================

# -----------------------------------------------------------------------------
# REQUIRED API KEYS (all three needed to play)
# -----------------------------------------------------------------------------

# OpenAI API key - Powers mystery generation, LLM responses, and Whisper transcription
# Get yours at: https://platform.openai.com/api-keys
OPENAI_API_KEY=

# ElevenLabs API key - Voice synthesis for characters and Game Master
# Get yours at: https://elevenlabs.io/
ELEVENLABS_API_KEY=

# HuggingFace token - 1990s adventure game style portraits and scenes
# Get yours at: https://huggingface.co/settings/tokens
HF_TOKEN=

# -----------------------------------------------------------------------------
# OPTIONAL: Performance Tuning
# -----------------------------------------------------------------------------

# Skip LLM prompt enhancement for images (saves ~8-13s per image)
# true = faster but lower quality prompts
# false = uses GPT-4o-mini to enhance prompts (default)
ENHANCE_PROMPTS=false

# -----------------------------------------------------------------------------
# OPTIONAL: Model Overrides
# -----------------------------------------------------------------------------

# Override models for specific tasks (defaults to gpt-4o-mini)
# SUSPECT_RESOLVER_MODEL=gpt-4o-mini
# LOCATION_RESOLVER_MODEL=gpt-4o-mini
